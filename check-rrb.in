#! /bin/sh
# Usage: check-rrb
# Takes no arguments, and runs all the test suites

set -e

top_srcdir=@top_srcdir_absolute@

TEST_SUITE_DIR=${top_srcdir}/test-suite
export TEST_SUITE_DIR

find $TEST_SUITE_DIR -maxdepth 1 -type f -executable | sh
